# 当前文件：保证容器内执行的脚本在 Windows 检出后仍是 LF。
# core.autocrlf=true 会把 *.sh 换成 CRLF，容器启动时 `#!/bin/sh\r` 会失败于
# "exec /usr/local/bin/...: no such file or directory"。
*.sh text eol=lf
代码评审报告
评审概要
本次变更大规模移除了 macOS、Linux 和 Docker 支持，将桌面端收缩为 Windows-only。涉及 CI 工作流、electron-builder 配置、UI 组件、i18n、测试、文档和安装脚本等 30+ 文件。

综合评分：85 / 100

无严重安全漏洞，但存在若干关键问题需要修复。

🔴 关键问题
1. README.md 与 release-draft-template.md 描述不一致
README.md 中 IMPORTANT 提示仍写着：

网盘链接仅提供 Windows 与 Apple silicon 的正式版安装包

但 release-draft-template.md 已修正为：

网盘链接仅提供 Windows 正式版安装包

macOS 支持已移除，README 不应再提及 Apple silicon，这会误导用户。

建议：将 README.md 第 139 行改为 网盘链接仅提供 Windows 正式版安装包，与 release-draft-template 保持一致。

2. 空条件渲染包装器 — 代码逻辑错误风险
LabSettingsModal.tsx 和 DesktopSettingsSubview.tsx 中，移除 !isLinux && 后留下了永远为真的空条件 {(：

// LabSettingsModal.tsx:388 — 永真条件，无意义
{(
    <div className={`flex items-center justify-between p-4 rounded-xl ...`} onClick={handleNativeBlurToggle}>

// DesktopSettingsSubview.tsx:440 — 同上
{(
    <div className="text-xs opacity-50 pt-1 text-left" ...>

这种写法虽然功能上等价于直接渲染，但：

可读性差，后续维护者会困惑为何有条件包装
可能导致不必要的 React fragment 嵌套
建议：移除 {( 和对应的 )} 包装，直接渲染内部 JSX 元素。

3. 生产代码中可能残留 Linux basic_text 加密回退逻辑
测试中移除了 createSafeStorage 的 backend 参数和 getSelectedStorageBackend 方法，以及 basic_text 相关测试用例。但本次 diff 未修改 生产代码 electron/kugouApiBridge.cjs 和 electron/qqAuthSessionRepository.cjs。

如果生产代码中仍存在对 safeStorage.getSelectedStorageBackend() 的调用和 basic_text 分支处理，则：

这些代码路径已成为死代码
Mock 与生产代码接口不一致，测试无法覆盖真实行为
建议：同步清理生产代码中 getSelectedStorageBackend 和 basic_text 相关分支，确保 Mock 与生产接口一致。

4. release 模板缺少文件末尾换行符
.github/release-candidate-template.md 和 .github/release-draft-template.md diff 显示 \ No newline at end of file。

缺少末尾换行符可能导致：

某些 CI 工具或 cat 命令拼接时输出异常
Git diff 产生不必要的噪音
建议：在两个模板文件末尾添加换行符。

🟡 中等问题（已按要求忽略次要问题，仅列出值得关注项）
5. vite.config.ts 中 define 对象格式不整洁
vite.config.ts 移除 __DOCKER_STACK_VERSION__ 后，在最后一个键值对和闭合大括号之间留有空行：

'__APP_RELEASE_CHANNEL__': JSON.stringify(appReleaseChannel),

},

建议：移除多余空行，保持对象格式整洁。

✅ 正面评价
CI 工作流清理彻底：4 个 workflow 的 matrix、artifact path、条件判断均同步更新，无遗漏
i18n 键值同步移除：macManualUpdateNotice / linuxManualUpdateNotice 在 en、zh-CN、in 三个语言文件中一致删除
安装脚本同步更新：install.sh 和 install.ps1 的菜单选项和分支逻辑均正确调整
package.json electron-builder 配置清理完整：mac、linux 段和 Linux 相关的 extraResources、npm scripts 均已移除
Skills 文档同步更新：所有 SKILL.md 中的 Docker/Linux 引用均已清理
评分明细
维度	得分	说明
安全漏洞	95	无注入/CSRF/RCE等漏洞；basic_text 测试移除略降安全感知
错误异常	80	空条件包装器虽不报错但属逻辑瑕疵；生产代码可能残留死代码
逻辑漏洞	85	README 与 release-draft 描述不一致会误导用户
代码质量	85	空条件包装、末尾换行缺失、多余空行
代码性能	95	移除多平台构建显著缩短 CI 时间
代码规范	80	部分文件格式不整洁
综合评分：85 / 100

